# Hello World


```sh
# dev build to _dist
deno run -A ../../utils/cli.ts dev

# Release build to _dist
deno run -A ../../utils/cli.ts

# Preview at http://localhost:8080 (But liverelaod cant work)
deno run -A ../../utils/preview.ts
```
